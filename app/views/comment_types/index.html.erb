<% if @admin_user %>
  <%= link_to 'Create New Type', new_comment_type_path %>
<% end %>

<div id="comment_typelist">
  <ul>
    <% @comment_types.each do |ct| %>
      <li><%= link_to ct.name, comment_type_path(ct) %></li>
    <% end %>
  </ul>
</div>
